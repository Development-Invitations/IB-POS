DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ib_pos_dev?schema=public"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="12h"
PORT=3000
# Не из исходного ТЗ — по прямому запросу клиента: ИИ-ассистент (чтение накладных) работает
# через Groq (https://console.groq.com/keys) — бесплатный тариф, быстрый инференс, есть vision
# для фото. Ключ получить в консоли Groq и вставить сюда.
GROQ_API_KEY=""
# GROQ_MODEL="qwen/qwen3.6-27b"
