# IB-POS — пошаговое ТЗ на разработку (рабочий документ)

Стек: **Tauri + React + TypeScript + Tailwind** (клиент), **Node.js/NestJS** (backend, локальный + облако), **PostgreSQL**, **Redis/RabbitMQ** (очередь синка), **Device Agent** (Node, при необходимости с .NET-обвязкой под конкретные SDK регистраторов).
Локализация: **RU, UZ (латиница), ЎЗ (кириллица)** — обязательны все три с самого MVP.

---

## Раздел 3. Роли и права доступа

Пять ролей в системе. У Администратора — полный доступ ко всем модулям без ограничений. У остальных — доступ только к тому, что относится к их работе.

| Модуль | Админ | Управляющий (Менеджер) | Зав. складом | Кассир | Бухгалтер |
|---|---|---|---|---|---|
| Продажа (чеки, оплата) | ✅ | ✅ | ❌ | ✅ | 👁 просмотр |
| Товары и категории | ✅ | ✅ | ✅ | 👁 просмотр | 👁 просмотр |
| Остатки / склад (приёмка, инвентаризация) | ✅ | 👁 просмотр | ✅ | ❌ | 👁 просмотр |
| Клиенты | ✅ | ✅ | ❌ | ✅ | 👁 просмотр |
| Скидки и акции (создание/редактирование) | ✅ | ✅ | ❌ | ❌ (только применяет в рамках лимита на экране продажи) | 👁 просмотр |
| Возвраты | ✅ подтверждает | ✅ подтверждает | ❌ | инициирует, требует подтверждения Управляющего/Админа | 👁 просмотр |
| Смены (открытие/закрытие) | ✅ | ✅ (все кассы) | ❌ | ✅ (только своя) | ❌ |
| Отчёты и аналитика | ✅ | ✅ (свои точки) | 👁 только по товарам/остаткам | 👁 только своя смена | ✅ полный доступ |
| Сотрудники и роли | ✅ | ❌ | ❌ | ❌ | ❌ |
| Интеграции (кассы, 1С) | ✅ | ❌ | ❌ | ❌ | ❌ |
| Оборудование | ✅ | 👁 статус устройств | ❌ | 👁 статус устройств | ❌ |
| Настройки системы, резервные копии | ✅ | ❌ | ❌ | ❌ | ❌ |

✅ — полный доступ (создание/редактирование/удаление), 👁 — только просмотр, ❌ — нет доступа.

Роли реализуются как `enum Role { CASHIER, MANAGER, WAREHOUSE, ADMIN, ACCOUNTANT }` на уровне БД (`apps/server/prisma/schema.prisma`) и проверяются через `RolesGuard` на каждом защищённом эндпоинте API — см. `apps/server/src/common/guards/roles.guard.ts` и декоратор `@Roles(...)`.

---

## Этап 0. Подготовка (1 неделя)

- [ ] Репозиторий, монорепо (`apps/client`, `apps/server`, `apps/device-agent`, `packages/shared`)
- [ ] CI (lint, typecheck, build), окружения dev/stage
- [ ] Схема БД v1 (см. ниже) в виде миграций
- [ ] i18n-инфраструктура: `packages/i18n` с ключами `ru`, `uz`, `uz-Cyrl`, линтер на «нет захардкоженных строк в UI»

**DoD:** пустой проект собирается, деплоится на стенд, переключение языка работает на заглушке.

---

## Этап 1. Ядро данных и API (2 недели)

Сущности: `organizations`, `stores`, `workstations` (кассы), `users`/`roles`, `products`, `categories`, `stock`, `customers`, `discounts`, `shifts`, `receipts`, `receipt_items`, `payments`, `integrations`, `devices`, `audit_log`.

- [ ] Мультитенантность: `organization_id` на всех таблицах
- [ ] REST API (NestJS): CRUD по товарам, категориям, клиентам, сменам
- [ ] Auth: логин/PIN для кассира, роли (Кассир / Управляющий / Администратор / Бухгалтер) — доступы как в разделе 3 ТЗ
- [ ] Офлайн-модель: локальная PostgreSQL/SQLite на точке + очередь исходящих событий (чек создан → отправить в облако / в фискального провайдера)

**DoD:** можно создать организацию, точку, кассу, товар, пользователя и получить их через API; работает без интернета (данные пишутся локально, встают в очередь).

**Дополнение (не было в исходном ТЗ, добавлено при подключении клиента к реальному API):**
Публичный `POST /organizations` (`{ name, admin: { fullName, login, password } }`, без авторизации) создаёт организацию, первого пользователя с ролью ADMIN и — обязательно — одну точку продаж и одну кассу «Касса 1» в той же транзакции. Без автосоздания кассы новый администратор физически не может открыть смену: экран выбора кассы показывает пустые списки без объяснения. На клиенте это экран `apps/client/src/components/RegisterScreen.tsx`, доступен по ссылке «Впервые здесь?» на экране входа (`LoginScreen.tsx`); после регистрации показывает ID организации (нужен для входа и пока не восстанавливается другим способом) и передаёт логин/ID обратно на экран входа.

---

## Этап 2. UI кассира — «Продажа» (2–3 недели)

Экран по макету (вариант 2, светлая тема):
- [ ] Хедер: лого, поиск (F2), касса/смена/кассир, уведомления
- [ ] Левая навигация (иконки, сворачиваемая)
- [ ] Категории товаров (табы) + сетка товаров (тач-зоны увеличены)
- [ ] Правая панель — текущий чек: позиции, +/−, удаление, скидка, итого
- [ ] Кнопка «Оплатить» — крупная, всегда видима
- [ ] Полноэкранный режим, адаптация 1366×768 → 1920×1080 (см. раздел 7 исходного ТЗ)
- [ ] **Локализация экрана на 3 языка** (переключатель RU / UZ / ЎЗ в хедере)

**DoD:** можно набрать чек касанием, применить скидку, увидеть итог — на всех трёх языках интерфейса.

*(Прототип этого экрана — во вложении, см. ниже)*

---

## Этап 3. Оплата и возвраты (1–2 недели)

- [ ] Модальное окно оплаты: наличные / карта / Click / Payme / QR / смешанная
- [ ] Расчёт сдачи
- [ ] Возврат чека / позиции с подтверждением правами
- [ ] Ошибка оплаты не должна терять чек (сохраняется локально, повтор)

**Дополнения (не было в исходном ТЗ):**
- Подтверждение возврата — не PIN-заглушка, а реальный вход управляющего/администратора через `POST /auth/login-pin` (логин + PIN) с проверкой роли на клиенте и повторной проверкой роли на сервере через `@Roles` у `POST /receipts/:id/return`.
- `Receipt.discountTotal` существовал в схеме с раннего этапа, но `create()` его не считал — скидка на экране продажи была чисто визуальной. `CreateReceiptDto` теперь принимает `discountPercent` (0–50), сервер считает `discountTotal`/`total` тем же способом, что и клиентское превью.

---

## Этап 4. Device Agent — оборудование (2 недели, параллельно с Этапом 2)

- [ ] Локальный сервис (localhost WS/HTTP) — отдельно от UI-процесса
- [ ] Сканер штрих-кодов → авто-добавление в чек, окно «Товар не найден»
- [ ] Фискальный регистратор (первый провод — под конкретную модель клиента)
- [ ] Денежный ящик, дисплей покупателя, POS-терминал
- [ ] Статус/тест устройства прямо из UI «Оборудование»

**DoD:** сканирование штрихкода на реальном сканере добавляет товар в чек; печатается тестовый чек на реальном регистраторе.

---

## Этап 5. Интеграции с виртуальными кассами (2–3 недели)

- [ ] Единый интерфейс адаптера (`connect / testConnection / sendReceipt / getStatus`)
- [ ] Reгos, Epos, SmartPos — первая волна (наиболее вероятные у первых клиентов)
- [ ] ArcaGroup, RahmatPos — вторая волна
- [ ] Очередь с ретраями, журнал ошибок, индикатор статуса фискализации на кассе

---

## Этап 6. Интеграция с 1С (2 недели)

- [ ] Коннектор CommerceML 2.0 (базовый режим, без доработки 1С со стороны клиента) — под «Бухгалтерию 3.0 для Узбекистана» и типовые конфигурации
- [ ] Экран сопоставления полей (номенклатура IB-POS ↔ справочник 1С) — т.к. у части клиентов другие конфигурации
- [ ] Опциональный режим HTTP-сервис/OData (для клиентов, готовых дорабатывать базу)
- [ ] Направления: 1С → IB-POS (товары/цены/остатки), IB-POS → 1С (свод продаж, возвраты)
- [ ] Карточка «1С» в разделе «Интеграции» (адрес базы, логин/токен, расписание, «Синхронизировать сейчас»)

---

## Этап 7. Товары, клиенты, скидки, сотрудники, смены (3 недели, частично параллельно)

- [ ] Модуль «Товары»: таблица + фильтры + карточка, импорт/экспорт
- [ ] Модуль «Клиенты»: поиск, история покупок, бонусы
- [ ] Модуль «Скидки и акции»: % / фикс / по товару / по категории, ограничение по роли
- [ ] Модуль «Сотрудники»: PIN, роли, история действий
- [ ] Модуль «Смены»: открытие/закрытие, внесение/изъятие, отчёт смены

**Статус клиента на 2026-08-28:**
- Товары — таблица, поиск, создание/редактирование, категория «на лету», деактивация/реактивация готовы (`apps/client/src/components/ProductsScreen.tsx`). Импорт/экспорт CSV есть только на backend (`GET/POST /products/export|import`), UI-кнопок в клиенте пока нет.
- Клиенты — список, поиск, создание/редактирование, история покупок, начисление/списание бонусов готовы (`CustomersScreen.tsx`). Бонусный баланс (`Customer.bonusBalance`) существовал в схеме с самого начала, но backend его не читал и не писал — добавлены `CustomersService.update()`/`adjustBonus()`.
- Скидки и акции — CRUD с областью действия (весь чек / товар / категория) и полем «минимальная роль» готовы (`DiscountsScreen.tsx`). **Важный незакрытый пробел:** созданные скидки нигде не применяются автоматически — на экране продажи по-прежнему только ручной ползунок % на весь чек (`discountPercent` в `POST /receipts`), а поле `minRole` нигде не проверяется. Модуль «Скидки» сейчас — это только справочник, не влияющий на кассу.
- Сотрудники, Смены (как отдельный модуль со списком/историей смен) — не начаты; в сайдбаре по-прежнему нет пункта «Сотрудники», а «Смены» — мёртвая кнопка (открытие/закрытие смены уже работает, но только как часть входа в кассу — см. `ShiftSetupScreen.tsx`/`CloseShiftModal.tsx`, не как отдельный экран с историей).

Также по всем трём модулям (Товары/Клиенты/Скидки) обнаружен и исправлен один и тот же класс бага: `Update*Dto extends PartialType(Create*Dto)` не включает `isActive`, поэтому деактивированную запись нельзя было включить обратно — глобальный `ValidationPipe({ whitelist: true })` тихо вырезал поле. Добавлено `isActive?: boolean` напрямую в `UpdateProductDto`, `UpdateDiscountDto` (у клиентов деактивации нет, только редактирование).

---

## Этап 8. Отчёты и облако (2–3 недели)

- [ ] Dashboard: продажи/чеки/средний чек/прибыль, график по часам
- [ ] Фильтр периода, экспорт Excel/CSV, печать
- [ ] Облачный бэкенд: мультиточки, сводные отчёты по сети (закладывается с 1-го этапа, но UI — здесь)
- [ ] Sync-worker точка ↔ облако, идемпотентность операций

---

## Этап 9. Настройки, резервные копии, безопасность (1 неделя)

- [ ] Общие настройки: название, валюта (UZS), язык по умолчанию
- [ ] Резервное копирование: ручное/авто, история, восстановление
- [ ] Налоги и фискализация: ставки, предупреждения при несовместимых параметрах
- [ ] Журнал событий/уведомлений

---

## Этап 10. Локализация — финальная проверка (сквозная, но отдельный чек-лист)

- [ ] Все статичные строки вынесены в i18n-словари (`ru.json`, `uz.json`, `uz-Cyrl.json`)
- [ ] Узбекская латиница: корректны спецсимволы **Oʻ, Gʻ** (не апостроф-заменитель)
- [ ] Узбекская кириллица: буквы **Ў, Қ, Ғ, Ҳ** — шрифт должен их поддерживать (проверить на моноблоке, не все системные шрифты Windows их корректно рендерят)
- [ ] Формат сумм: разделитель разрядов, UZS без десятичных
- [ ] Переключение языка не ломает вёрстку (узбекские строки в среднем длиннее русских — заложить запас в кнопках/лейблах)
- [ ] Пилотный прогон на реальном моноблоке с сенсорным экраном на всех 3 языках

**DoD:** кассир может работать полную смену на любом из трёх языков без переключения на английский/системные дефолты.

---

## Общий график (ориентир)

| Этап | Недели |
|---|---|
| 0. Подготовка | 1 |
| 1. Ядро данных/API | 2 |
| 2. UI кассира | 2–3 |
| 3. Оплата/возвраты | 1–2 |
| 4. Device Agent | 2 (параллельно) |
| 5. Интеграции касс | 2–3 |
| 6. Интеграция 1С | 2 |
| 7. Товары/клиенты/скидки/сотрудники/смены | 3 |
| 8. Отчёты и облако | 2–3 |
| 9. Настройки/бэкапы | 1 |
| 10. Локализация — финал | сквозной + 1 неделя на прогон |

**Итого MVP (этапы 0–5, минимально жизнеспособная касса с одной фискальной интеграцией):** ~10–12 недель.
**Полная версия (все модули + 1С + облако + 3 языка):** ~18–20 недель.
